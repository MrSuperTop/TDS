import pygame

# * Settings
windowSize = (1200, 720)
FPS = 60
drawColliderBorders = False

# * Constants
colors = {
  'white': (255, 255, 255)
}

# * Variables
window = pygame.display.set_mode(list(windowSize))
